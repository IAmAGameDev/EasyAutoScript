namespace EasyAutoScript
{
    public enum TokenType
    {
        OpenParenthesis,
        CloseParenthesis,

        Identifier,

        // Values
        Boolean,
        Number,
        String,

        // Statements
        Sleep,
        Write,

        // Expressions
    }
}